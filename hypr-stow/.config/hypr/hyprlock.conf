# ============================================================
#               H Y P R L O C K   N E O C Y B E R   V3
#       Immersive Glass-Blur Box + Integrated Info Panel
# ============================================================

# === BACKGROUND (NO GLOBAL BLUR) ===
background {
    monitor =
    path = /home/dmann/realm/builds/dotfiles/wallpapers/sunflowers.png
    blur_passes = 0
    contrast = 1.0
    brightness = 0.92
    vibrancy = 0.9
    vibrancy_darkness = 0.15
}

# === GENERAL SETTINGS ===
general {
    no_fade_in = false
    grace = 1000
    disable_loading_bar = true
}

# === MAIN GLASS CONTAINER (FULL BLUR FOCUS AREA) ===
shape {
    monitor =
    size = 500, 450
    color = rgba(255, 255, 255, 0.08)
    rounding = 25
    border_size = 1
    border_color = rgba(255, 255, 255, 0.25)
    blur_passes = 10             # Heavy blur for cinematic glass effect
    position = 0, 30
    halign = center
    valign = center
}

# === PROFILE IMAGE ===
image {
    monitor =
    path = /home/dmann/realm/builds/dotfiles/wallpapers/pinkgirl.png
    border_size = 3
    border_color = rgba(255, 105, 180, 0.85)
    size = 80
    rounding = 999
    position = 0, 180
    halign = center
    valign = center
}

# === NAME ===
label {
    monitor =
    text = dmann
    color = rgba(27, 27, 27, 0.95)
    font_size = 22
    font_family = JetBrainsMono Nerd Font
    position = 0, 100
    halign = center
    valign = center
}

# === TIME ===
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I:%M %p")</span>"
    color = rgba(27, 27, 27, 0.95)
    font_size = 70
    font_family = JetBrainsMono Nerd Font
    position = 0, 25
    halign = center
    valign = center
}

# === DATE ===
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%A, %B %d, %Y")"
    color = rgba(27, 27, 27, 0.8)
    font_size = 18
    font_family = JetBrainsMono Nerd Font
    position = 0, -30
    halign = center
    valign = center
}

# === DIVIDER LINE ===
shape {
    monitor =
    size = 160, 2
    color = rgba(76, 161, 27, 0.84)
    rounding = 1
    position = 0, -65
    halign = center
    valign = center
}

# === USER LABEL ===
label {
    monitor =
    text =   $USER
    color = rgba(27, 27, 27, 0.85)
    font_size = 15
    font_family = JetBrainsMono Nerd Font
    position = 0, -100
    halign = center
    valign = center
}

# === PASSWORD INPUT FIELD (INSIDE BLUR AREA) ===
input-field {
    monitor =
    size = 340, 58
    outline_thickness = 1
    outer_color = rgba(255, 255, 255, 0.15)
    inner_color = rgba(255, 255, 255, 0.1)
    font_color = rgb(0, 0, 0)
    font_family = JetBrainsMono Nerd Font
    placeholder_text = <i><span foreground="#ffffff99">🔒  Enter Access Code</span></i>
    hide_input = true
    rounding = 12
    position = 0, -165
    halign = center
    valign = center
    fade_on_empty = true
    dots_size = 0.25
    dots_spacing = 0.25
    dots_center = true
}

